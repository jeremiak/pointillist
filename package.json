{
  "name": "pointillist",
  "version": "1.0.0",
  "description": "A humble library for pop art illustration with pure CSS",
  "main": "index.js",
  "scripts": {
    "start": "beefy index.js -o bundle.js --live",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jeremiak",
  "license": "ISC",
  "devDependencies": {
    "beefy": "^2.1.5",
    "browserify": "^10.1.3",
    "watchify": "^3.2.1"
  }
}
